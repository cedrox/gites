title: Gite de Mer
author:
  name: Gite de Mer
  email: info@gitedemer.net

# The `>` after `description:` means to ignore line-breaks until next key.
# If you want to omit the line-break after the end of text, use `>-` instead.
description: >
  Location de gite de mer en Normandie. 3 Gites idéalement situés à 2 pas de la mer. De 4 à 8 personnes.

# Build settings

theme: minima

plugins:
  - jekyll-feed
  - jekyll-seo-tag

# Theme-specific settings

minima:
  # Minima date format.
  # Refer to https://shopify.github.io/liquid/filters/date/ if you want to customize this.
  #
  # date_format: "%b %-d, %Y"

  # Generate social links in footer.
  #
  social_links:
    - { platform: facebook,       user_url: "https://www.facebook.com/gitedemer" }

# If you want to link only specific pages in your header, uncomment this and add the path to the pages in
# order as they should show up.
#
header_pages:
  - about.md
  - Bigorneau.md
  - Crevette.md
  - EtoileDeMer.md

# Set to `true` to show excerpts on the homepage.
#
# show_excerpts: false
